server:
  port: 8083

spring:
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

# Bật Feign + Fallback (Spring Cloud 2020+)
feign:
  circuitbreaker:
    enabled: true
